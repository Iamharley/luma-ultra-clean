const express = require("express"); const app = express(); const PORT = process.env.PORT || 3000; app.get("/", (req, res) => { res.send("�� LUMA ULTRA CLEAN - OK"); }); app.get("/health", (req, res) => { res.json({ status: "healthy", service: "LUMA Ultra Clean Server", timestamp: new Date().toISOString(), port: PORT }); }); app.listen(PORT, "0.0.0.0", () => { console.log("🚀 LUMA Ultra Clean Server démarré"); console.log(`🌐 Port: ${PORT}`); console.log("✅ Health check: /health"); console.log("✅ Ready for Railway"); });
